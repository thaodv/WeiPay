/**
 * Contact Class is used to instantiate any contact objects throughout the app.
 */
class Contact {
    constructor(name, address, tokenList) {
      this.name = name;
      this.address = address;
      this.tokenList = tokenList;
    }
  }
  
  export default Contact;
  